# Toxic880/tap

Homebrew tap for [TwistPad](https://github.com/Toxic880/TwistPad), which turns a
twist of two fingers on your trackpad into a volume knob.

```bash
brew install --cask Toxic880/tap/twistpad
```

Signed and notarized, so it opens with no Gatekeeper warnings.
